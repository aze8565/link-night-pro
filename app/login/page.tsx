import { login, signup } from "./actions";
export default async function LoginPage({searchParams}:{searchParams:Promise<{error?:string;message?:string;next?:string}>}){const p=await searchParams;return <main className="auth-wrap"><div className="card auth-card"><p className="eyebrow">ACCOUNT</p><h1>登录霖K夜娱地图</h1>{p.error&&<p className="notice error">{p.error}</p>}{p.message&&<p className="notice success">{p.message}</p>}<form className="form"><input type="hidden" name="next" value={p.next??"/account"}/><div className="field"><label>邮箱</label><input className="input" name="email" type="email" required/></div><div className="field"><label>密码</label><input className="input" name="password" type="password" minLength={8} required/></div><div className="hero-actions"><button className="btn btn-primary" formAction={login}>登录</button><button className="btn" formAction={signup}>注册</button></div></form></div></main>}

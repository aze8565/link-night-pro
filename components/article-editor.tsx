"use client";
import { useMemo, useState } from "react";
import { createClient } from "@/lib/supabase/client";
import type { Article, AccessLevel, ArticleStatus } from "@/lib/types";
import { Markdown } from "@/components/markdown";

type Draft={title:string;slug:string;excerpt:string;content:string;country:string;city:string;cover_url:string|null;access_level:AccessLevel;status:ArticleStatus};
const blank:Draft={title:"",slug:"",excerpt:"",content:"",country:"",city:"",cover_url:null,access_level:"free",status:"draft"};
export function ArticleEditor({initial}:{initial?:Article}){const [draft,setDraft]=useState<Draft>(initial?{title:initial.title,slug:initial.slug,excerpt:initial.excerpt,content:initial.content,country:initial.country,city:initial.city,cover_url:initial.cover_url,access_level:initial.access_level,status:initial.status}:blank);const [busy,setBusy]=useState(false);const [preview,setPreview]=useState(false);const update=(key:keyof Draft,value:string|null)=>setDraft(x=>({...x,[key]:value}));const generatedSlug=useMemo(()=>draft.slug||draft.title.trim().toLowerCase().replace(/\s+/g,"-"),[draft.slug,draft.title]);async function upload(file:File){setBusy(true);try{const supabase=createClient();const ext=file.name.split(".").pop();const path=`covers/${crypto.randomUUID()}.${ext}`;const {error}=await supabase.storage.from("cover-images").upload(path,file,{upsert:false});if(error)throw error;const {data}=supabase.storage.from("cover-images").getPublicUrl(path);update("cover_url",data.publicUrl)}catch(e){alert(e instanceof Error?e.message:"上传失败")}finally{setBusy(false)}}async function save(status:ArticleStatus){setBusy(true);try{const payload={...draft,slug:generatedSlug,status};const url=initial?`/api/admin/articles/${initial.id}`:"/api/admin/articles";const r=await fetch(url,{method:initial?"PATCH":"POST",headers:{"content-type":"application/json"},body:JSON.stringify(payload)});const j=await r.json();if(!r.ok)throw new Error(j.error??"保存失败");location.href="/admin/articles"}catch(e){alert(e instanceof Error?e.message:"保存失败")}finally{setBusy(false)}}return <div className="editor-grid"><div>{preview?<div className="markdown-preview"><h1>{draft.title||"未命名文章"}</h1><p className="muted">{draft.excerpt}</p><Markdown content={draft.content}/></div>:<div className="form"><div className="field"><label>标题</label><input className="input" value={draft.title} onChange={e=>update("title",e.target.value)} placeholder="例如：第一次去曼谷，先看懂这三个区域"/></div><div className="field"><label>网址别名（可留空自动生成）</label><input className="input" value={draft.slug} onChange={e=>update("slug",e.target.value)} placeholder={generatedSlug}/></div><div className="field"><label>摘要</label><textarea className="input" rows={4} value={draft.excerpt} onChange={e=>update("excerpt",e.target.value)} placeholder="用于首页卡片和搜索摘要"/></div><div className="field"><label>正文（Markdown）</label><textarea className="textarea" value={draft.content} onChange={e=>update("content",e.target.value)} placeholder={'## 一、区域结构\n\n正文内容...'} /></div></div>}</div><aside className="editor-side"><div className="card"><div className="field"><label>国家</label><input className="input" value={draft.country} onChange={e=>update("country",e.target.value)} placeholder="泰国"/></div><div className="field"><label>城市</label><input className="input" value={draft.city} onChange={e=>update("city",e.target.value)} placeholder="曼谷"/></div><div className="field"><label>会员权限</label><select className="select" value={draft.access_level} onChange={e=>update("access_level",e.target.value)}><option value="free">免费</option><option value="regional">区域会员</option><option value="global">全球会员</option></select></div></div><div className="card"><div className="field"><label>封面图</label><input className="input" type="file" accept="image/*" onChange={e=>e.target.files?.[0]&&upload(e.target.files[0])}/>{draft.cover_url&&<img className="image-preview" src={draft.cover_url} alt="封面预览"/>}</div></div><button className="btn" onClick={()=>setPreview(!preview)}>{preview?"返回编辑":"预览正文"}</button><button className="btn" disabled={busy} onClick={()=>save("draft")}>保存草稿</button><button className="btn btn-primary" disabled={busy} onClick={()=>save("published")}>{busy?"处理中...":"发布文章"}</button></aside></div>}
